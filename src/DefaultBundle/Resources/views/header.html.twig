<div class="brand">
    {{ 'title.firstline' | trans({}, 'main') }}<br />
    {{ 'title.secondline' | trans({}, 'main') }}
</div>
<div class="locale-bar">
    {{ include('locale.html.twig') }}
</div>

<nav class="navbar navbar-default" role="navigation">
    <div class="navbar-header">
        {{ include('locale.html.twig') }}
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="{{ path('homepage') }}">
            {{ 'title.short' | trans({}, 'main') }}
        </a>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
            <li>
                <a href="{{ path('homepage') }}">{{ 'title' | trans({}, 'home') }}</a>
            </li>
            <li>
                <a href="{{ path('presentation') }}">{{ 'title' | trans({}, 'presentation') }}</a>
            </li>
            <li>
                <a href="{{ path('schedule') }}">{{ 'title' | trans({}, 'schedule') }}</a>
            </li>
            <li>
                <a href="{{ path('prizes') }}">{{ 'title' | trans({}, 'prizes') }}</a>
            </li>
            <li>
                <a href="{{ path('partners') }}">{{ 'title' | trans({}, 'partners') }}</a>
            </li>
            <li>
                <a href="{{ path('contact') }}">{{ 'title' | trans({}, 'contact') }}</a>
            </li>
        </ul>
    </div>
</nav>
